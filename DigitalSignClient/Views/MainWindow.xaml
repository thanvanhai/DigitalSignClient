<Window x:Class="DigitalSignClient.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Digital Sign Client" Height="700" Width="1200"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.8"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Background="#2196F3" Height="70">
            <Grid Margin="20,0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Digital Sign Client" 
                               FontSize="22" 
                               FontWeight="Bold" 
                               Foreground="White"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <TextBlock Text="{Binding CurrentUser.FullName}" 
                               FontSize="15" Foreground="White" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <Button Content="Đăng xuất" Command="{Binding LogoutCommand}" Style="{StaticResource MenuButtonStyle}" Background="Transparent" Foreground="White" BorderBrush="White" BorderThickness="1"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <DockPanel Grid.Row="1">

            <!-- Sidebar -->
            <StackPanel DockPanel.Dock="Left" Width="200" Background="#F5F5F5">
                <Button Content="Dashboard" Command="{Binding ShowDashboardCommand}" Style="{StaticResource MenuButtonStyle}"/>
                <Button Content="Quản lý loại tài liệu" Command="{Binding ShowDocumentTypeCommand}" Style="{StaticResource MenuButtonStyle}"/>
                <Button Content="Workflow Template" Command="{Binding ShowWorkflowCommand}" Style="{StaticResource MenuButtonStyle}"/>
                <Button Content="Danh sách văn bản" Command="{Binding ShowDocumentListCommand}" Style="{StaticResource MenuButtonStyle}"/>
            </StackPanel>

            <!-- Content -->
            <ContentControl Content="{Binding CurrentView}" />
        </DockPanel>
    </Grid>
</Window>
